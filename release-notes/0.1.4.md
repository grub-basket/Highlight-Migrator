# 0.1.4

Store-review fix, no behaviour change.

## Changes

- Replace direct `element.style` assignments (the colour swatch background and the preview chip colour in the review tab) with `setCssStyles(...)`. This resolves the community-store lint error `obsidianmd/no-static-styles-assignment`. The review tab looks and behaves exactly as before.
