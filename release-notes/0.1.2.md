# 0.1.2

Maintenance release addressing store-review feedback.

## Changes

- Remove two unused `catch` error bindings in the migration code (the "`'e' is defined but never used`" review finding). The blocks that deliberately ignore an error now use an optional catch binding.
